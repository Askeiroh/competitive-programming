#include <bits/stdc++.h>
using namespace std;

#define int long long
#define endl "\n"

int N, W;
vector<pair<int, int>> infoItems;

void solve() {
    cin >> N >> W;
    infoItems.
    for(int i = 0; i  < N; i++) {
        
    }
}

int32_t main() {
    ios_base::sync_with_stdio(0);cin.tie(0);
    int TC = 0;
    if(TC) {
        cin >> TC;
        while(TC--) { solve(); }
    } else { solve(); }
}